Delete one symbol from the bound local checkout only when resolved references,
callers, type relations, overrides/exports, durable cross-codebase consumers,
and configured dynamic-use patterns permit it. The server validates an internal
plan and semctl immediately applies it with lease, generation, path, and hash
checks. Unsafe or incomplete cases refuse before any write. This mutating tool
uses the host's normal approval path.
