Rename one unambiguous qualified or positional target and its exact semantic
references in the bound local checkout. The server builds and validates the
grammar-native plan; semctl immediately rechecks its lease, generation, paths,
and hashes before applying it transactionally. Comments, strings, unresolved
text, uncertain sites, and formatter execution remain explicit options. This is
a mutating tool and goes through the host's normal approval path.
