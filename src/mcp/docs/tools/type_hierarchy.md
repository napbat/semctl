Traverse supertypes, subtypes, or both to a bounded depth. Returns stable
qualified identities, external nodes, relation kind, and whether each edge is
declared, structural, or unknown. Use `implementations_of` for only the direct
reverse-implements result.
