The detected project graph for this codebase — leaf projects (one buildable unit each, rooted at a manifest like Cargo.toml / *.csproj / package.json / go.mod) and the groups that contain them (Cargo/npm/go workspaces, .NET solutions).

Use it to orient in a multi-project repo before scoping other queries, and to confirm what the indexer detected. Each leaf shows its kind, root, and indexed file count; each group lists its members.
