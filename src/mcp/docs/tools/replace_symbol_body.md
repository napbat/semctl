Replace the grammar-owned body of one unambiguous declaration in the bound local
checkout while preserving its declaration, documentation, attributes,
surrounding trivia, and line endings. The server reparses and re-resolves an
internal plan; semctl immediately hash-verifies and transactionally applies it.
This mutating tool uses the host's normal approval path.
