List every codebase visible in the active tenant, including id, slug, source
kind, access scope, indexed revision, graph generation/freshness, and local
checkout binding. Use this to discover a codebase selector; it never widens
authorization and never exposes credentials.
