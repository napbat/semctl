Return definitions with no resolved incoming references, the exact reason, and
the graph-completeness caveat. Treat results conservatively when reflection,
generated code, or unsupported languages may call the symbol.
