Insert a complete declaration at the grammar-owned boundary after an
unambiguous target in the bound local checkout. The server validates indentation,
the enclosing container, parsing, and binding in an internal plan; semctl then
hash-verifies and transactionally applies it. This mutating tool uses the host's
normal approval path.
